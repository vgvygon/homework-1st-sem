name: Display current dateTime

on: [push, pull_request]

jobs:
  show-datetime:
    name: Show current date and time
    runs-on: ubuntu-latest

    steps:
      - name: Print dateTime to console
        run: |
          echo "=== CURRENT DATE AND TIME ==="
          echo "Local time: $(date)"
          echo "UTC time: $(date -u)"name: Print DateTime

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  show-time:
    runs-on: ubuntu-latest
    steps:
      - name: Print current datetimename: Display current dateTime

on: [push, pull_request]

jobs:
  show-datetime:
    name: Show current date and time
    runs-on: ubuntu-latest

    steps:
      - name: Print dateTime to console
        run: |
          echo "=== CURRENT DATE AND TIME ==="
          echo "Local time: $(date)"
          echo "UTC time: $(date -u)"
          echo "ISO 8601 format: $(date -Iseconds)"
          echo "Unix timestamp: $(date +%s)"
          echo "Timezone: $(date +%Z)"
          echo "GitHub event: ${{ github.event_name }}"
          echo "Branch: ${{ github.ref }}"
          echo "============================="
        run: echo "Time: $(date)"

          echo "ISO 8601 format: $(date -Iseconds)"
          echo "Unix timestamp: $(date +%s)"
          echo "Timezone: $(date +%Z)"
          echo "GitHub event: ${{ github.event_name }}"
          echo "Branch: ${{ github.ref }}"
          echo "============================="
